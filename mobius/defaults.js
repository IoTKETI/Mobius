/**
 * @file 리소스 기본값 상수. 의존성 없는 순수 모듈이다.
 */

'use strict';

// et 를 지정하지 않고 만든 리소스의 만료 시각 (UTC).
//
// 예전에는 "생성 시각 + 2년" 이었다. 그러면 만료 스윕이 제대로 도는 순간
// 2년 전에 만든 리소스가 소리 없이 지워진다 — 클라이언트가 et 를 명시하지
// 않았을 뿐인데 보관을 포기한 것으로 취급된 셈이다.
//
// 기본값은 "사실상 만료하지 않음" 이어야 한다. 만료를 원하면 생성 시
// et 를 명시하면 된다.
//
// oneM2M 의 et 는 'YYYYMMDDTHHmmss' 형식이고 문자열 비교로 대소를 가린다.
exports.DEFAULT_ET = '20991231T235959';
