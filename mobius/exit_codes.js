'use strict';
// 전용 종료 코드.
//
// **1 은 쓸 수 없다.** backstop(잡히지 않은 예외)과 app.js 의 fail_start(DB 연결
// 실패)가 이미 1 로 나간다. 마스터가 1 을 보고 "포트 충돌이다" 로 종료하면 DB 가
// 몇 초 늦어 죽은 워커에도 마스터가 자살한다.
exports.PORT_TAKEN = 12;   // 포트를 남이 쥐고 있다. 마스터도 같은 코드로 즉시 종료한다 (§5.1)
exports.NO_CONF = 13;      // 워커가 conf.json 을 못 찾았다(운영 중 지워짐). 마스터도 종료한다 (§4.5.1 나)
